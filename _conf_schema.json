{
    "enable_input_check": {
        "description": "是否启用用户输入检测",
        "type": "bool",
        "default": false,
        "hint": "开启后将在用户发送消息后、调用LLM前检测输入内容，不合规则直接拦截"
    },
    "input_check_provider": {
        "description": "输入检测使用的大模型",
        "type": "string",
        "_special": "select_provider",
        "default": "",
        "hint": "不选择则使用当前会话默认的大模型"
    },
    "output_check_provider": {
        "description": "输出检测使用的大模型",
        "type": "string",
        "_special": "select_provider",
        "default": "",
        "hint": "不选择则使用当前会话默认的大模型"
    },
    "banned_regex": {
        "description": "违禁词正则表达式列表",
        "type": "list",
        "items": {
            "type": "string"
        },
        "default": [],
        "hint": "每行一个正则表达式，匹配到的内容将被拦截，不区分大小写"
    },
    "allowed_regex": {
        "description": "白名单正则表达式列表",
        "type": "list",
        "items": {
            "type": "string"
        },
        "default": [],
        "hint": "匹配到的内容将跳过违禁词检测，优先级高于违禁词规则"
    },
    "allowed_expressions": {
        "description": "合规的表达列表",
        "type": "list",
        "items": {
            "type": "string"
        },
        "default": [],
        "hint": "格式为「词汇~原因」，用~分隔词汇和合规原因，例如「我去~表达惊讶」，这些内容会被加入白名单并告知大模型为合规内容"
    },
    "ignore_sessions": {
        "description": "忽略检测的会话列表",
        "type": "list",
        "items": {
            "type": "string"
        },
        "default": [],
        "hint": "填写会话的 unified_msg_origin，可通过 /sid 命令获取，列表内的会话将跳过所有检测"
    },
    "blocked_message": {
        "description": "拦截后的自定义回复内容",
        "type": "text",
        "default": "该内容不合规，已被拦截。",
        "hint": "内容被拦截时，将发送这段内容给用户"
    },
    "check_prompt": {
        "description": "大模型合规检测系统提示词",
        "type": "text",
        "default": "你是专业的内容合规审核员，需严格检查内容是否符合中国法律法规与公序良俗。不合规内容包括但不限于：色情低俗、暴力恐怖、诈骗诱导、违法违规、政治敏感、人身攻击、歧视辱骂等。必须严格按照以下格式回复，禁止额外内容：1. 内容合规：仅回复「合规」2. 内容不合规：仅回复「不合规|违规类型」，例如「不合规|色情低俗」",
        "hint": "用于自定义大模型的审核规则，非必要不修改"
    }
}
